<h1 class="p-5 text-light shadow-1-strong rounded mb-3">Items List</h1>

<%=form_with model: @item, local: true do |f| %>
    <div class="p-1 text-light text-center shadow-1-strong rounded mb-1">
        <h3>Filter items by Category</h3>
            <%@categories.each do |category|%>
                <a href="/pages/categories/<%=category.id%>"class="btn btn-primary btn-sm"><%=category.name%></a>
            <%end%>
    </div> 
<%end%>

<%#using the item card partial to render item cards %>
<%= render "item_card" %>

